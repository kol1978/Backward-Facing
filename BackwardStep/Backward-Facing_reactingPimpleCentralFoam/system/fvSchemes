/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  2.3.0                                 |
|   \\  /    A nd           | Web:      www.OpenFOAM.org                      |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      fvSchemes;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

ddtSchemes
{
    default         Euler;
}

gradSchemes
{
    default         Gauss linear;
}

defaultAdvScheme     vanLeer;
defaultAdvSchemeV    vanLeerV;

divSchemes
{
    default         none;

    //momentum equation
    div(phi_nei,U)	Gauss $defaultAdvSchemeV;
    div(phi_own,U)	Gauss $defaultAdvSchemeV;
    div(((rho*nuEff)*dev2(T(grad(U))))) Gauss linear;

    //energy equation
    div(phi_nei,h)	Gauss $defaultAdvScheme;
    div(phi_own,h)	Gauss $defaultAdvScheme;
    div(phi_nei,K)	Gauss $defaultAdvScheme;
    div(phi_own,K)	Gauss $defaultAdvScheme;

    //continuity equation
    div(phid_nei,p)	Gauss $defaultAdvScheme;
    div(phid_own,p)	Gauss $defaultAdvScheme;

    //for multicomponent solvers
    div(alphaEff*hi,grad(Yi)) Gauss linear;
    div(rhoi*Uri,hi)    Gauss linear;
}

laplacianSchemes
{
    default         Gauss linear corrected;
}

interpolationSchemes
{
    default         none;

    interpolate((rho*U))	linear;
    interpolate(((rho*nut)+thermo:mu)) linear;

    reconstruct(rho)		$defaultAdvScheme;
    reconstruct(psi)		$defaultAdvScheme;
    reconstruct(p)		$defaultAdvScheme;
    reconstruct(U)		$defaultAdvSchemeV;
    reconstruct(rho*U)		$defaultAdvSchemeV;
    reconstruct(Dp)		$defaultAdvScheme;
    reconstruct(rhoHat)		$defaultAdvScheme;
    reconstruct(W)		$defaultAdvScheme;
}

snGradSchemes
{
    default         corrected;
}

fluxRequired
{
    default     none;
    p;
    h;
}

/*
ddtSchemes
{
    default         CrankNicolson 0.9;   // 2-й порядок, мало диссипации
    // или backward; — тоже 2-й порядок
}

gradSchemes
{
    default         Gauss linear;
    grad(U)         cellLimited Gauss linear 1;  // ограничитель для устойчивости
}

divSchemes
{
    default         none;

    // Конвекция скорости — центральная схема (минимум диссипации)
    div(phi,U)      Gauss linear;

    // Турбулентность
    div(phi,k)      Gauss linear;
    div(phi,B)      Gauss linear;
    div(B)          Gauss linear;

    // Энергия и species
    div(phi,h)      Gauss linear;
    div(phi,Yi_h)   Gauss linear;
    div(phi,K)      Gauss linear;

    // Диффузия
    div(tauMC)      Gauss linear;
    div((muDev*dev2(T(grad(U))))) Gauss linear;
}

laplacianSchemes
{
    default         Gauss linear corrected;
}

interpolationSchemes
{
    default         linear;
}

snGradSchemes
{
    default         corrected;
}
*/

// ************************************************************************* //
/*
 Ключевое отличие от высокоскоростной настройки: Gauss linear вместо Gauss vanLeer / Gass KurganovTadmor для всех конвективных членов. linear — чисто центральная схема 2-го порядка, недиссипативная, как в стандартном reactingFoam.
 turbulenceProperties — LES
Для разрешения турбулентных пульсаций нужен LES, а не RANS
 **/
